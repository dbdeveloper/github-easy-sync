1. додати інтеграційний тест: підряд, через 300мсек - робити 3-5 sync - має з'явитись кілька записів в .push-queue, і вони всі до кінця Драін повинні зникнути, а на тестовому github має з'явитись усі ці файли, які передавались в цих sync-ах.
2. такий самий тест, але зміни (одна зміна - один рядок) вносяться в один і той самий файл - має з'явитись 3-5 комітів де ПОСЛІДОВНО буде збільшуватись число рядків в цьому файлі на github. Перевірити останні 3-5 комітів цього файлу на github і переконатись що нічого не пропало
3. питання: а можна розфарбовувати назви файлів в дереві Obsidian з plugin? - наприклад, якийсь plugin буде сірим кольором писати назви файлів, які знаходяться в .gitignore ?
4. чи можна додати в контекстне меню <root> і інших каталогів ".gitignore" - відкривати і редагувати .gitignore для цього каталогу. Якщо .gitignore не існує - створювати його. Якщо користувач закриває його без змін (порожній) - не зберігати зміни (тобто не зберігати сам файл, тобто - видаляти). Якось так... Не зрозуміло, як видаляти .gitignore з каталогу. Можливо, якщо користувач видалив з нього УСІ записи (він порожній, хіба що пробіли та LF є) - тоді його автоматично видаляти? Але так, як .gitignore за цим сценаріє видаляється автоматично, коли спорожніє - його немає змісту класти в .trash/ (він і так вже порожній), і головна надія на його відновлення - тільки з github repo (бо .gitignore потрапляють в repo, а отже потраплять в deleted list нашого plugin).
5. 